apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Container-to-container, so the internal port 9090 rather than the 9091 published
    # to the host.
    url: http://prometheus:9090
    isDefault: true
    editable: false
